{
  "name": "tonydefazio-com",
  "private": true,
  "version": "1.5.1",
  "description": "Landing page for tonydefazio.com — routes visitors to the five project sites.",
  "scripts": {
    "serve": "python3 -m http.server 5099 --bind 127.0.0.1 --directory site",
    "dry": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
